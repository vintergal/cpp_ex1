//vintergal@gmail.com
#include "algorithms.hpp"
using namespace graph;
namespace graph
{
    algorithms::algorithms(/* args */)
    {
    }

    algorithms::~algorithms()
    {
    }

    graph algorithms::bfs(int vetrice, const graph& g)
    {
        return graph(g);
    }

    graph algorithms::dijkstra(int vetrice, const graph& g)
    {
        return graph(g);
    }

    graph algorithms::prim(const graph& g)
    {
        return graph(g);
    }

    graph algorithms::kruskal(const graph& g)
    {
        return graph(g);
    }

    
}